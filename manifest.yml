applications:
  - name: build-your-solace-agent-mesh
    buildpacks:
      - nodejs_buildpack
    memory: 512M
    instances: 1
    random-route: false
    command: npm start
    env:
      NODE_ENV: production
      NPM_CONFIG_PRODUCTION: false
